# Python 文件管理系统 — 项目结构说明

---

## 一、项目概述

本项目是一个**基于命令行的文件管理系统**，用纯 Python 编写，单文件实现（`pythonfinal.py`，共 626 行）。提供文件的创建、删除、重命名、复制、移动、搜索、统计等常用操作，并内置了**回收站机制**（软删除 + 可还原）。

**v2 新增**：全局导航功能 — 在任何输入提示下输入 `0` 返回主菜单，输入 `b` 返回上一步。

---

## 二、调用库及函数说明

### 2.1 标准库导入

| 模块 | 导入方式 | 使用的函数/类 | 用途 |
|------|----------|---------------|------|
| `os` | `import os` | `os.getcwd()` | 获取当前工作目录 |
| | | `os.path.exists()` | 判断路径是否存在 |
| | | `os.path.isabs()` | 判断是否为绝对路径 |
| | | `os.path.join()` | 拼接路径 |
| | | `os.path.basename()` | 提取文件名 |
| | | `os.path.dirname()` | 提取父目录 |
| | | `os.path.splitext()` | 分离文件名与扩展名 |
| | | `os.path.abspath()` | 转为绝对路径 |
| | | `os.path.isdir()` / `os.path.isfile()` | 判断路径类型 |
| | | `os.makedirs(..., exist_ok=True)` | 递归创建目录 |
| | | `os.rename()` | 重命名文件/目录 |
| | | `os.remove()` | 删除文件 |
| | | `os.stat()` | 获取文件元信息（大小、时间戳） |
| | | `os.walk()` | 递归遍历目录树 |
| | | `os.path.getsize()` | 获取文件大小 |
| | | `os.path.expanduser("~")` | 展开用户主目录路径 |
| | | `os.sep` | 路径分隔符 |
| `shutil` | `import shutil` | `shutil.move()` | 移动文件/目录（剪切） |
| | | `shutil.copy2()` | 复制文件（保留元数据） |
| | | `shutil.copytree()` | 递归复制整个目录 |
| | | `shutil.rmtree()` | 递归删除整个目录 |
| `json` | `import json` | `json.dump()` | 将 Python 对象序列化写入文件 |
| | | `json.load()` | 从文件读取 JSON 反序列化 |
| `datetime` | `import datetime` | `datetime.datetime.now()` | 获取当前时间 |
| | | `.strftime()` | 格式化时间为字符串 |
| | | `datetime.datetime.fromtimestamp()` | 将时间戳转为 datetime 对象 |
| `glob` | `import glob` | `glob.fnmatch.fnmatch()` | Unix shell 风格通配符匹配（如 `*.txt`） |
| `pathlib` | `from pathlib import Path` | （导入但本项目中未直接使用） | — |

### 2.2 关键函数调用关系图

```
main()
 ├── create_file()          → os.makedirs, open(), input_multiline(), safe_input()
 ├── delete_file()          → shutil.move, ensure_recycle_bin, load/save_recycle_meta, get_timestamp, safe_input()
 ├── rename_file()          → os.rename, safe_input()
 ├── copy_move_file()       → shutil.copy2, shutil.copytree, shutil.move, safe_input()
 ├── create_directory()     → os.makedirs, safe_input()
 ├── delete_directory()     → shutil.move, ensure_recycle_bin, load/save_recycle_meta, safe_input()
 ├── traverse_directory()   → os.walk, safe_input()
 ├── file_search()          → os.walk, glob.fnmatch.fnmatch, open(), safe_input()
 ├── file_statistics()      → os.stat, os.walk, open(), safe_input()
 ├── recycle_bin_menu()     → safe_input()  ─┐
 │   ├── list_recycle_bin()  ├→ load_recycle_meta
 │   ├── restore_from_recycle() → shutil.move, load/save_recycle_meta, safe_input()
 │   ├── empty_recycle_bin() → shutil.rmtree, os.remove, load/save_recycle_meta, safe_input()
 │   └── permanent_delete_from_recycle() → 同上 + safe_input()
 └── 切换工作目录            → os.path.isdir, os.path.abspath, safe_input()
```

---

## 三、项目结构（模块划分）

```
pythonfinal.py                    # 唯一的源文件，按功能分为 7 大区块
│
├── [区块 0] 全局变量与工具函数      (第 1-64 行)
│   ├── BACK                       # 导航哨兵：返回上一步
│   ├── MAIN_MENU                  # 导航哨兵：返回主菜单
│   ├── safe_input(prompt)         # 带导航提示的输入函数（拦截 0/b）
│   ├── current_dir               # 当前工作目录
│   ├── RECYCLE_BIN               # 回收站物理路径 ~/.recycle_bin/
│   ├── RECYCLE_META              # 回收站元数据文件路径 ~/.recycle_bin/recycle_meta.json
│   ├── ensure_recycle_bin()      # 确保回收站目录和元数据文件存在
│   ├── load_recycle_meta()       # 读取回收站元数据（返回 list）
│   ├── save_recycle_meta(meta)   # 保存回收站元数据到 JSON
│   ├── get_timestamp()           # 生成时间戳字符串 "YYYYMMDD_HHMMSS"
│   └── input_multiline(prompt)   # 多行文本输入（空行结束，支持 --menu/--back）
│
├── [区块 1] 创建文件               (第 67-96 行)
│   └── create_file()             # 新建文本文件，支持导航回退
│
├── [区块 2] 删除文件（移至回收站）   (第 99-129 行)
│   └── delete_file()             # 将文件移到回收站并记录元数据，支持导航回退
│
├── [区块 3] 重命名                 (第 132-159 行)
│   └── rename_file()             # 重命名文件或目录，支持分步回退
│
├── [区块 4] 复制/移动              (第 162-210 行)
│   └── copy_move_file()          # 状态机三步操作（选择→源→目标），每步可回退
│
├── [区块 5] 创建目录               (第 213-226 行)
│   └── create_directory()        # 递归创建目录，支持导航回退
│
├── [区块 6] 删除目录（移至回收站）   (第 229-259 行)
│   └── delete_directory()        # 将目录移到回收站并记录元数据，支持导航回退
│
├── [区块 7] 遍历目录               (第 262-285 行)
│   └── traverse_directory()      # 打印目录树（树形缩进），支持导航回退
│
├── [区块 8] 文件搜索               (第 288-360 行)
│   └── file_search()             # 状态机多步搜索（类型→目录→模式/关键词），每步可回退
│
├── [区块 9] 文件统计               (第 363-430 行)
│   └── file_statistics()         # 显示文件/目录详细信息，支持导航回退
│
├── [区块 10] 回收站管理            (第 433-575 行)
│   ├── recycle_bin_menu()        # 回收站子菜单（循环），支持导航回退
│   ├── list_recycle_bin()        # 列出回收站所有条目
│   ├── restore_from_recycle()    # 按编号还原，b 回回收站菜单，0 回主菜单
│   ├── empty_recycle_bin()       # 清空全部，确认步骤可回退
│   └── permanent_delete_from_recycle()  # 永久删除单个，两步均可回退
│
└── [区块 11] 主菜单与入口           (第 578-626 行)
    └── main()                    # 主循环，分发到各功能函数，显示导航提示
```

---

## 四、各功能模块详细实现

### 4.1 工具函数层

#### 导航控制常量 — `BACK` / `MAIN_MENU`
- **功能**：作为哨兵值（`object()` 实例），用于在各函数间传递导航指令
- **`BACK`**：表示用户请求返回上一步（当前函数内回退或返回上级菜单）
- **`MAIN_MENU`**：表示用户请求直接返回主菜单（跨多层函数穿透传播）

#### `safe_input(prompt)` → 返回 `str | BACK | MAIN_MENU`
- **功能**：`input()` 的包装函数，统一拦截全局导航命令
- **提示格式**：`{prompt} (0:主菜单, b:上一步): `
- **规则**：输入 `0` 返回 `MAIN_MENU`；输入 `b` 返回 `BACK`；其他返回原始字符串
- **设计**：`BACK`/`MAIN_MENU` 为 `object()` 实例，永不与用户输入冲突

#### `ensure_recycle_bin()`
- **功能**：确保 `~/.recycle_bin/` 目录和 `recycle_meta.json` 文件存在
- **实现**：调用 `os.makedirs()` 建目录，若 JSON 文件不存在则初始化为空数组 `[]`

#### `load_recycle_meta()` → 返回 `list`
- **功能**：读取回收站元数据
- **实现**：`json.load()` 读取，返回包含 `{original_path, deleted_at, bin_name}` 字典的列表

#### `save_recycle_meta(meta)`
- **功能**：将元数据列表写回 JSON 文件
- **实现**：`json.dump()` 写入，`ensure_ascii=False` 支持中文路径，`indent=2` 可读

#### `get_timestamp()` → 返回 `str`
- **功能**：生成格式为 `"YYYYMMDD_HHMMSS"` 的时间戳
- **用途**：附加到回收站文件名上，避免重名冲突

#### `input_multiline(prompt)` → 返回 `str | BACK | MAIN_MENU`
- **功能**：允许用户输入多行文本，遇到空行结束
- **导航**：输入 `--menu` 返回 `MAIN_MENU`；输入 `--back` 返回 `BACK`
- **实现**：`while True` 循环读取 `input()`，遇 `""` 跳出，用 `"\n".join(lines)` 拼接

### 4.2 文件创建 — `create_file()`
1. 提示输入文件名（`safe_input`，支持 0/b 导航）
2. 若为相对路径则拼接到 `current_dir`
3. 检查是否已存在 → 冲突则重新输入
4. 调用 `input_multiline()` 获取文件内容（支持 --menu/--back）
5. 内容步骤按 b 回到文件名输入
6. `os.makedirs()` 确保父目录存在（递归创建）
7. `open(path, "w", encoding="utf-8")` 写入内容

### 4.3 文件删除（软删除） — `delete_file()`
1. 输入文件名（`safe_input`，0/b 返回主菜单），解析为绝对/相对路径
2. 检查存在性，若是目录则拒绝（提示使用目录删除功能）
3. 调用 `ensure_recycle_bin()` 保证回收站可用
4. 生成带时间戳的文件名：`原文件名_时间戳.扩展名`
5. `shutil.move()` 将文件移入回收站
6. 在元数据中追加记录 `{original_path, deleted_at, bin_name}`
7. `save_recycle_meta()` 持久化

### 4.4 重命名 — `rename_file()`
1. 输入源路径（`safe_input`），0 回主菜单，b 回主菜单，路径不存在则重新输入
2. 输入新文件名（`safe_input`），b 回到源路径输入
3. 检查源存在、新文件名非空、目标不存在
4. `os.rename()` 执行重命名

### 4.5 复制/移动 — `copy_move_file()`
- **状态机设计**（`step` 变量控制三步流程）：
  - step 1：选择 1=复制 / 2=移动，b 回主菜单
  - step 2：输入源路径，b 回到 step 1
  - step 3：输入目标路径，b 回到 step 2
- 复制时：
  - 目录 → `shutil.copytree()` 递归复制
  - 文件 → `os.makedirs()` 确保目标父目录存在 + `shutil.copy2()` 保留元数据
- 移动时：`shutil.move()`

### 4.6 目录创建 — `create_directory()`
- 输入目录名（`safe_input`），0/b 返回主菜单，直接调用 `os.makedirs(path, exist_ok=True)` 递归创建

### 4.7 目录删除（软删除） — `delete_directory()`
- 逻辑与 `delete_file()` 一致，但操作对象是目录，回收站文件名不含扩展名

### 4.8 目录遍历 — `traverse_directory()`
1. 输入要遍历的目录（`safe_input`，回车默认当前目录，0/b 返回主菜单）
2. 使用 `os.walk()` 递归遍历
3. 按树形结构输出：
   - `level` 通过计算路径分隔符数量得到
   - 每级缩进 `"│   "`，分支符号 `"├── "`
   - 文件夹后缀 `/`

### 4.9 文件搜索 — `file_search()`
- **状态机设计**（三步流程）：
  - step 1：选择搜索类型（按文件名/按内容），b 回主菜单
  - step 2：输入起始目录，b 回到 step 1
  - step 3：输入模式或关键词，b 回到 step 2
- **按文件名搜索**：
  - 输入通配符模式（如 `*.py`）
  - `os.walk()` 遍历，每个文件名用 `glob.fnmatch.fnmatch(f, pattern)` 匹配
- **按内容搜索**：
  - 输入关键词
  - 遍历所有文件，尝试 `open()` 读取
  - 逐行检查 `keyword in line`，匹配则打印文件路径 + 行号 + 前 80 字符
  - 不可读文件（权限/二进制）跳过 `except (PermissionError, OSError)`

### 4.10 文件统计 — `file_statistics()`
1. 输入路径（`safe_input`，0/b 返回主菜单）
2. `os.stat()` 获取文件元信息
3. 输出：路径、类型、大小（字节/KB）、创建/修改/访问时间
4. 对文件：提取扩展名，若为文本文件则统计行数（`sum(1 for _ in f)`）
5. 对目录：`os.walk()` 递归统计文件数、子目录数、总大小

### 4.11 回收站管理 — `recycle_bin_menu()`
- 独立子菜单循环（使用 `safe_input`），包含 4 个功能：
- 输入 0 或 b 均返回主菜单
- 各子函数若返回 `MAIN_MENU`，则穿透返回主菜单

1. **查看** `list_recycle_bin()`：遍历元数据列表，打印编号、回收站名、原始路径、删除时间
2. **还原** `restore_from_recycle()`：
   - 列出回收站 → 输入编号（`safe_input`，0 回主菜单，b 回回收站菜单）
   - 检查回收站文件存在性、原始路径是否已被占用
   - `os.makedirs()` 重建原始父目录
   - `shutil.move()` 移回原路径
   - 从元数据列表移除该条并保存
3. **清空** `empty_recycle_bin()`：
   - 确认步骤使用 `safe_input`（0 回主菜单，b 回回收站菜单）
   - 要求输入 `"yes"` 确认
   - 遍历元数据，目录用 `shutil.rmtree()`，文件用 `os.remove()`
   - 保存空数组 `[]`
4. **永久删除单个** `permanent_delete_from_recycle()`：
   - 两步输入均使用 `safe_input`：编号输入（b 回回收站菜单），确认（b 回编号输入）
   - 同上但只处理一个条目

### 4.12 主菜单 — `main()`
- `while True` 无限循环，显示菜单和当前工作目录
- 底部显示全局导航提示：`在任何输入界面输入 0 返回主菜单，输入 b 返回上一步`
- 用户输入选择（主菜单使用原生 `input()`，因为 0 已用于退出），`if/elif` 分支分发到各功能函数
- 额外功能：**切换工作目录**（选项 11，使用 `safe_input` 支持导航），修改全局变量 `current_dir`
- `KeyboardInterrupt` 捕获防止 Ctrl+C 直接退出

---

## 五、数据流与架构设计

### 5.1 全局状态

```
current_dir (str)          → 当前工作目录，所有相对路径操作的基准
RECYCLE_BIN (str)          → "~/.recycle_bin/" 物理存储
RECYCLE_META (str)         → "~/.recycle_bin/recycle_meta.json" 元数据
BACK (object)              → 导航哨兵：返回上一步
MAIN_MENU (object)         → 导航哨兵：返回主菜单
```

### 5.2 回收站数据格式

```json
[
  {
    "original_path": "/home/user/docs/report.txt",
    "deleted_at": "20260606_143025",
    "bin_name": "report_20260606_143025.txt"
  }
]
```

### 5.3 设计特点

| 设计点 | 说明 |
|--------|------|
| **全局导航** | `safe_input()` 统一拦截所有用户输入，输入 `0` 返回主菜单，输入 `b` 返回上一步；多步函数采用状态机模式，每步独立可回退 |
| **导航穿透** | `MAIN_MENU` 哨兵可跨多层函数传播，确保在深层子菜单中也能一键回到主界面 |
| **软删除机制** | 删除操作不真正删除，而是 `shutil.move` 到回收站，元数据存 JSON |
| **时间戳防冲突** | 删除时文件名追加时间戳，避免回收站内同名覆盖 |
| **宽字符支持** | 所有文件读写使用 `encoding="utf-8"`，JSON 使用 `ensure_ascii=False` |
| **健壮性** | 各处 `try/except` 捕获异常并打印错误信息而非崩溃 |
| **中断友好** | 遍历/搜索等长操作用 `KeyboardInterrupt` 捕获，部分结果仍展示 |
| **路径灵活** | 同时支持相对路径（基于 `current_dir`）和绝对路径输入 |

---

## 六、函数清单（共 21 个）

| 序号 | 函数名 | 参数 | 返回值 | 行号 |
|------|--------|------|--------|------|
| 1 | `ensure_recycle_bin()` | 无 | 无 | 26 |
| 2 | `load_recycle_meta()` | 无 | `list` | 34 |
| 3 | `save_recycle_meta(meta)` | `meta: list` | 无 | 40 |
| 4 | `get_timestamp()` | 无 | `str` | 47 |
| 5 | `input_multiline(prompt)` | `prompt: str` | `str \| BACK \| MAIN_MENU` | 51 |
| 6 | `safe_input(prompt)` | `prompt: str` | `str \| BACK \| MAIN_MENU` | 12 |
| 7 | `create_file()` | 无 | 无 | 67 |
| 8 | `delete_file()` | 无 | 无 | 99 |
| 9 | `rename_file()` | 无 | 无 | 132 |
| 10 | `copy_move_file()` | 无 | 无 | 162 |
| 11 | `create_directory()` | 无 | 无 | 213 |
| 12 | `delete_directory()` | 无 | 无 | 229 |
| 13 | `traverse_directory()` | 无 | 无 | 262 |
| 14 | `file_search()` | 无 | 无 | 288 |
| 15 | `file_statistics()` | 无 | 无 | 363 |
| 16 | `recycle_bin_menu()` | 无 | 无 | 433 |
| 17 | `list_recycle_bin()` | 无 | 无 | 459 |
| 18 | `restore_from_recycle()` | 无 | `None \| MAIN_MENU` | 469 |
| 19 | `empty_recycle_bin()` | 无 | `None \| MAIN_MENU` | 509 |
| 20 | `permanent_delete_from_recycle()` | 无 | `None \| MAIN_MENU` | 537 |
| 21 | `main()` | 无 | 无 | 578 |

---

## 七、执行流程图

```
程序入口: if __name__ == "__main__": main()
                │
                ▼
┌──────────────────────────────────────────────────┐
│  main() 主循环                                    │
│  while True:                                     │
│    显示菜单 + 当前目录 + 导航提示 (0:主菜单, b:上一步) │
│    等待用户输入 (1-11, 0)                          │
│    if/elif 分支跳转                                │
└──────────┬───────────────────────────────────────┘
           │
   ┌───────┼──────────────────────────────────────────────┐
   │       │                                              │
   ▼       ▼                                              ▼
 1-9    10. 回收站管理                                  0. 退出
 直接    进入 recycle_bin_menu() 子循环                   break
 执行     ├── 查看                                       结束程序
 返回     ├── 还原 → safe_input 支持 0 回主菜单 / b 回上级
         ├── 清空 → safe_input 确认，0/b 导航
         └── 永久删除单个 → 两步 safe_input，均可导航

全局导航规则:
  • 所有 safe_input() 提示末尾显示 "(0:主菜单, b:上一步)"
  • 输入 0 → 返回 MAIN_MENU 哨兵，跨层穿透至主菜单
  • 输入 b → 返回 BACK 哨兵，当前函数内回退一步或返回上级菜单
  • 多步函数 (rename, copy_move, file_search, permanent_delete)
    采用状态机 + while 循环实现步间回退
```

---
